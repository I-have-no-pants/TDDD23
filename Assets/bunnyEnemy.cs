using UnityEngine;
using System.Collections;

public class bunnyEnemy : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	//rigidbody.AddForce((transform.position-GameObject.FindGameObjectWithTag("Bunny").transform.position).normalized*(-10));
		
	}
}
